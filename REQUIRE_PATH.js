
const top_dir = __dirname
/**@enum */
const PATH = {
  top:            `${top_dir}`,
  bin:            `${top_dir}/bin`,
  config:         `${top_dir}/config`,
  configure:      `${top_dir}/config/configure`,
  moduler:        `${top_dir}/config/moduler`,
  hanger:         `${top_dir}/hanger`,
  modules:        `${top_dir}/modules`,
  routes:         `${top_dir}/routes`,
  test:           `${top_dir}/test`,
};
module.exports.PATH = PATH